{
  "dependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.7"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon ./server/src/index.mjs",
    "start": "cross-env NODE_ENV=production node ./server/src/index.mjs",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
  }
}
